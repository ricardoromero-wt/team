# Knowledge Index

> Read the index at session start. Read individual files when the trigger conditions are met.

## Always Loaded

These govern every decision — read before starting any task.

_(Add 1-3 core reference files here as your domain knowledge grows. Candidates for Team: a brief-template reference for stack-specific subagents, and a cross-stack-conventions audit. These are read on every task.)_

## Triggered by Content

| Domain | File | Read When |
|--------|------|-----------|
<!-- Add rows as you create knowledge files in docs/knowledge/.
     Each file covers one focused domain topic — stable, curated, human-reviewed.
     Use file-path or work-domain triggers, not abstract domain descriptions.
     Examples Team will likely add as it grows:
     | NestJS subagent dispatch | `subagent-nest-brief.md` | Dispatching work to the Nest monorepo subagent |
     | Python subagent dispatch | `subagent-python-brief.md` | Dispatching work to the Python/React SPA subagent |
     | Cross-stack conventions | `cross-stack-conventions.md` | Proposing a new convention or noticing divergence between stacks |
     | Backend flow specs | `flow-specs/<flow-name>.md` | Touching a service involved in a documented end-to-end flow |
     | NestJS gotchas | `nestjs-gotchas.md` | Dispatch involves NestJS DI scope, decorators, or module boundaries |
     | Python framework reference | `python-framework-ref.md` | Dispatch involves FastAPI/Django/SQLAlchemy idioms |
     | React SPA sanity gates | `spa-sanity-gates.md` | Reviewing a SPA PR for the type/build/smoke gate |
-->

Knowledge base entries (solved problems) live in `docs/solutions/` across these categories: architecture | debugging | integration | tooling | performance | subagent-design | nestjs | python-react. Search there when a problem resembles something previously encountered.
