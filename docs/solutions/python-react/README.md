# python-react

Python service and React SPA-specific gotchas, patterns, and configuration — framework idioms (FastAPI, Django, SQLAlchemy), SPA build setup, type-check gates, smoke E2E patterns, contract boundary between the two.

## Entries

_No entries yet. This category will grow through operation._
